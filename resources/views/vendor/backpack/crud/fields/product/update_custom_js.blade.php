@push('after_scripts')
<script type="text/javascript">

    

    $( document ).ready(function() {

        /* let isServiceField = $('.is_service_checkbox')
        let widthField = $('input[name=width]')
        let heightField = $('input[name=height]')
        let depthField = $('input[name=depth]')
        let weightField = $('input[name=weight]')

        checkIsServiceField()

        isServiceField.change( function() {
            checkIsServiceField()
        })  

        function checkIsServiceField() {
            if (isServiceField.prop('checked') ) {
                disabledFields()
            } else {
                enableFields()
            }
        }

        function enableFields() {
            widthField.attr('disabled', false)
            heightField.attr('disabled', false)
            depthField.attr('disabled', false)
            weightField.attr('disabled', false)
        }

        function disabledFields() {
            widthField.attr('disabled', true)
            heightField.attr('disabled', true)
            depthField.attr('disabled', true)
            weightField.attr('disabled', true)
        } */

    });

</script>
@endpush 